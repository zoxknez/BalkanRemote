name: Ingest Official Job Feeds (DISABLED)

on:
  # Keep only manual trigger; no schedule
  workflow_dispatch: {}

jobs:
  ingest:
    if: false  # disabled; use Vercel Cron /api/ingest/official instead
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow is disabled. Use Vercel Cron to call /api/ingest/official."
